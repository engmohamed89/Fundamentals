#include <iostream>
#include <a
using namespace std;

int main()
{
   
}

void calc(int a, int b,int op){
    if(op == 1){
        sum(a,b);
    }
    elseif (op==2)
    {
        sub(a,b);
    }
    elseif(op == 3)
    {
        mul(a,b)
    }
}